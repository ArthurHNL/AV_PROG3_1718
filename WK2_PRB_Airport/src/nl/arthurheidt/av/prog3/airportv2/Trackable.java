package nl.arthurheidt.av.prog3.airportv2;

public interface Trackable {
    // int xPos;
    // int yPos;
    // NOT legal because an interface has no method bodies so the instance variables
    // could not be used anyway.

    public void showInfoOnRadar();
}
